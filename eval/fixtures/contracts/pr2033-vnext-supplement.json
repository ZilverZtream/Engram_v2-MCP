{
  "version": 1,
  "candidates": [
    {
      "id": "VNEXT-SESSION-PRINCIPAL-BINDING",
      "kind": "hard_obligation",
      "text": "bind any server-session or cached security baseline to the current request principal identity and define fail-closed behavior for a missing owner, owner mismatch, principal mismatch, or username change",
      "source_path": "../../../crates/engram_server/src/handlers/planning_tools.rs",
      "source_contains": "bind any server-session or cached security baseline to the current request principal identity and define fail-closed behavior for a missing owner, owner mismatch, principal mismatch, or username change"
    },
    {
      "id": "VNEXT-AUTHORIZATION-SYNC",
      "kind": "obligation",
      "text": "identify tenant sync events and other account, role, or permission synchronization paths; define the observable behavior while propagation is delayed, stale, skipped, retried, or permanently failed",
      "source_path": "../../../crates/engram_server/src/handlers/planning_tools.rs",
      "source_contains": "identify tenant sync events and other account, role, or permission synchronization paths; define the observable behavior while propagation is delayed, stale, skipped, retried, or permanently failed"
    },
    {
      "id": "VNEXT-SCOPE-DECISION-FEEDBACK",
      "kind": "obligation",
      "text": "preserve the established user-facing message, status, and redirect contract for each tenant decision or scope-selection flag, including disabled, missing, and inaccessible targets",
      "source_path": "../../../crates/engram_server/src/handlers/planning_tools.rs",
      "source_contains": "preserve the established user-facing message, status, and redirect contract for each tenant decision or scope-selection flag, including disabled, missing, and inaccessible targets"
    }
  ]
}
