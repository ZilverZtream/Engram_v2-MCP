# Engram engineering readiness

Updated 2026-09-13. Scores are provisional engineering judgments, not measured
accuracy percentages or CTO certification. Product behavior and regression
fixtures remain generic; the reference repository is evaluation data only.

A 9 requires repeatable supported behavior, actionable partial-coverage reporting
and no known high-impact false-success case within the tested scope. A 10 also
requires broader retrieval and end-to-end agent evidence. Test counts alone do
not increase a score.

| Priority | Workflow | Current | Remaining gap |
|---|---|---:|---|
| 1 | Search and chunk retrieval | 10 | V68 repairs literal punctuation, short Unicode routing and snapshot filtering. Real MCP finds all seven expected files for three queries and recovers all seven chunks directly. A sealed hard historical intake replay then used those source and chunk contracts in a 95/100 result, up from 87/100 before the generic planning hardening |
| 2 | Method edit context | 9 | Class-qualified TypeScript lookup and colliding parser names are repaired, including actual source-exact historical acceptance; broader dynamic-call recall and end-to-end benefit remain partial |
| 3 | Symbol references | 9 | Qualified binding and existing-index repair verified; runtime dispatch is outside static proof |
| 4 | Guards and settings | 9 | Scope, ambiguity and boundary regressions verified; runtime authorization remains external |
| 5 | Page context | 9 | Static composition verified; runtime page behavior remains external |
| 6 | Schema and SQL validation | 9 | Supported CTE/subquery/DML/ORDER BY binding verified; broader SQL execution contracts remain partial |
| 7 | Business rules | 9 | V73 fresh Luna/v17 review found 0 unsupported claims across 10 rules and 10 surrounding prose fields. Source-expression fidelity, enclosing data-flow guards and distinct guarded helper calls are repaired; wider-language and end-to-end accepted-behavior evidence remain the gap to 10 |
| 8 | Merged-work exemplars | 9 | Stored-diff repair and complete recovery arguments verified; lexical recall and separately imported approval evidence remain explicit limits |
| 9 | Test derivation | 9 | Property ownership, markup companions and exact-class nullable-field observations verified through generic tests and live MCP. Native reviewers consumed the matrix; complete dynamic role/state/runtime coverage remains external |
| 10 | Pre-commit review | 9 | Empty immune/anti-pattern corpora now produce degraded coverage; input/source identity fixes verified. Runtime execution and broader semantic review remain external |

## Current candidate and deployment

The 2026-09-13 historical PR replay supplies the first strong end-to-end intake
evidence for the current workflow. An independent fixed-rubric scorer rated the
hardened Engram arm **95/100**, up from **87/100** on the same sealed base and
accepted reference. Exact file-cohort recall improved from 11/22 to 14/22 and
semantic-role recall from approximately 17/22 to 19/22. Product behavior scored
20/20, evidence integrity 5/5, and unknown classification 10/10. The replay did
not misreport any resolved or `wontFix` review item as an active defect.

The generic candidate reconciles pre-move temporal paths, reads current and
legacy GIS metadata, recovers deterministic DBML-layout and nearest SQL-project
companions even when they are intentionally absent from the source index, adds
archive, derived-state, browser-resource, caller-owned-context, wire-contract and
XML-encoding test axes, and supports an inclusive historical PR boundary before
review parsing or storage. Caller-supplied change intent is explicitly labelled
as unapproved evidence. A compiled MCP smoke recovered exact-cased unindexed
companions and verified the corrected intent header. This result establishes
intake usefulness on one difficult historical change; matched implementation and
runtime acceptance remain future evidence rather than assumptions.

The 2026-09-13 generator-provenance followup is installed from `109eda3` with
server SHA-256 `85168AFEB9EEE1835DD164BB5C5625B9ED0C71E6E4FB9656E7E59366AC23804F`.
It moves next-ten workflow `validate_generated_code` from 8 to 9 after a real
Visual Studio generator receipt passed the deployed tool. The other nine
first-list workflows remain 9; this evidence improves the implementation workflow without
claiming runtime or product-behavior proof.

V73 was the business-rule candidate and its exact server SHA-256 (`571802484e04acd556ad04093410859c00d8af7de9da5ee1c73535f8a73f1787`)
matched its accepted candidate. Its acceptance receipt passed with all 147 schemas,
generation 4 and the sealed corpus preserved. The independent semantic review of
two fresh Luna/v17 extractions classified the 20 reviewed rule and prose units as
3 directly entailed, 17 properly qualified and 0 unsupported. V17 preserves exact
source expressions, enclosing data-flow guards and distinct call-site guards.
Business Rules therefore moves to 9 within the tested scope; the bounded sample,
unexamined dynamic helper internals and end-to-end accepted-behavior evidence keep
it below 10.

V69 was the preceding documented candidate and passed deployment acceptance with 220 focused checks. It adds
explicit citation recovery calls to search and grep, corrects the temporal
follow-up tool name, preserves individual grouped evidence, and separates
historical co-change leads from required edits. Business prompt v13 separates
null values from omitted arguments and helper intent from verified behavior.
Actual MCP acceptance preserves all 147 schemas, generation 4 and the stored
replay corpus, directly recovers advertised search/grep citations, and verifies
24 temporal members across three resource groups. The original comparator failure
(neutral resource files excluded) is retained alongside a corrected offline
review of the same responses. Live generation 1263, config, rendered memory/rules
and application source were preserved; all three owned clients closed with exit 0.
Fresh Luna/v13 produced 11 supported or qualified rules and removed both prior
prose overclaims. Twelve manufactured qualified references remain in data-flow
prose and are diagnosed. Business Rules stays 8; v14 source-expression fidelity
and optional exact quote verification are now under test for V70.

The fresh direct Fable distiller probe supplied the full canonical role and four
preloads, retrieved five search hits and read eight primary guidance documents.
Its ordinary chunk call was denied by the probe's stricter citation-mode rule;
only one of five labeled quotations was exact. This exposes citation ergonomics
and quotation fidelity gaps, not a production API failure or a measured paired
workflow advantage. The initial incomplete input-delivery run is retained too.

V68 was the preceding installed release and passed deployment acceptance. The combined candidate has
99 reviewed Rust checks (92 unchanged business/UI checks plus seven current
optimized search checks). Actual MCP acceptance recovered all seven source hits
for each of two previously failing XML literals and an anchored regex, then
recovered every chunk directly by its returned document identity. All 147 schemas,
existing sealed review semantics, generation 4 and the complete stored replay
corpus were preserved. Live deployment preserved generation 1263, configuration,
rendered memory/rules and application source; all three owned clients closed
successfully. Search returns to 9. Fresh Luna/v12 extraction passed persistence and
exact chunk recovery. Root and independent review agree that all 11 rules are
supported or qualified; independent review identified two unsupported surrounding
prose claims (argument omission and helper behavior). Business Rules remains 8
while the generic v13 correction is tested. This is product acceptance against the recorded workflow,
not a new paired implementation result.

V65 was the preceding installed release and passed live deployment acceptance. It includes the V64
branch-context correction and a narrow UI-parser compatibility fix. Eighty-five
Rust checks passed (64 unchanged business/identity checks and 21 final UI checks),
followed by release compilation and an actual MCP comparison. The UI gate ran
without degradation and removed the false unchanged-class novelty finding; all
non-class findings, 147 schemas, sealed source and the complete stored replay
corpus remained unchanged. No additional novel class appeared in that sealed
patch, so positive novelty retention is covered by the generic tests, not claimed
from this replay. Live deployment preserved generation 1263, configuration,
rendered stored memory/rules and application source. Two fresh Luna/v11
extractions passed exact persistence/chunk recovery. Root reviewed all 12 rules:
3 entailed local statements, 8 qualified, and 1 unsupported sufficient implication.
Every rule now retains its enclosing access condition, fixing the prior three
omissions in this sample. A new partial-query-predicate implication remains;
Business Rules stays 8. This is one fresh sample, not a statistical prompt benchmark.
A generic bounded predicate-coverage correction passed 92 reviewed Rust checks
and V67 actual MCP compatibility acceptance, retaining the sealed corpus and
review results. It has not been deployed or tested with a fresh extraction yet.
V66 was held after an XML-literal counterexample; V67 excludes that unsupported
syntax and tests the inclusive declaration-size boundary.

V68 combines that correction with a search repair. A generic real-index test
reproduced the exact XML-literal miss, then exposed missing snapshot-generation
filtering. Raw trigram candidate queries, character-based short-pattern routing
and the existing namespace generation policy now pass the functional regression.
The debug speed matrix failed; all seven optimized search checks subsequently
passed. The 1,000-file matrix measured 15–17 ms for Engram versus 40–43 ms for
ripgrep (local engine timing, excluding MCP and agent overhead). Actual MCP
retrieval acceptance is pending. Benchmark comparisons now distinguish literal and regex
queries and check matching result counts. New benchmark fixtures own their
temporary directories. Those candidate checks preceded V68 deployment; current scores and acceptance are recorded above.

V64 compiled but failed actual MCP acceptance and was not deployed. Its focused evidence comprises
84 Rust checks: 64 unchanged business/identity checks plus 20 checks against the
final UI/page-context correction. The published sidecar passed 12 branch-context
cases, 43 return reports across 46 requests, and 18 parameter cases. Branch
diagnostics compare exact enclosing-condition tokens only with a rule's structured
WHEN, preserving literal case and excluding mentions confined to strings, THEN or
refs. Missing spelling triggers rule-scoped review, not a semantic verdict.
Malformed, stale or ambiguous context cannot establish an absent guard. UI checks
use unchanged source spans as precedent and disclose unknown class inventories.
The real markup contains tag-level server expressions between attributes, which
the new scanner rejected. The class finding disappeared because the check was
degraded, so acceptance correctly failed. Both clients closed and stored corpus
fingerprints matched. A narrow compatibility follow-up excludes dynamically
attributed tags as class evidence while retaining unrelated static tags; it must
pass actual source and MCP checks before deployment. These test results alone do
not raise a score; the corrected V65 acceptance is recorded above.

V63 was the preceding installed release and passed live deployment acceptance. Only the VB sidecar
changed; the server, Luna configuration, generation 1263, stored knowledge and
application source were preserved. Nine newly supported single-line If cases,
43 return-path reports, 18 parameter cases and 20 relevant Rust tests passed.
Actual MCP compatibility retained all 147 schemas and the frozen replay corpus.
An independent current-source comparison verified five return histories in one
method; a second method correctly remains unavailable because of its lambda.

Two fresh Luna/v10 extractions were persisted and recovered through exact indexed
identities and hash-bound chunk reads. Independent review found ten supported or
qualified rules and three unsupported standalone implications: the latter omit an
enclosing access condition. Empty per-rule warning lists did not catch this, and
global unverified labels do not repair it. Business Rules remains 8. The next
generic correction supplies source-bound branch ancestry independently of whole
method return-path coverage, then flags missing literal enclosing guards without
claiming semantic equivalence or silently rewriting model output.

V62 was the preceding installed release and passed live deployment acceptance, preserving generation 1263,
configuration, parser binary, stored knowledge and application source. It passed
152 focused tests and release compilation. A post-seal MCP comparison removed the
comment-as-string finding while preserving three genuine string-literal findings,
all 147 schemas and the complete stored replay corpus. The initial acceptance
assertion overlooked grouped findings; its failure and a log-directory setup
failure are preserved alongside the successful evidence recheck. Reviews were not
rerun merely to replace those harness failures.

Both independent live regressions passed: the nullable field in a file containing
an unrelated nested class is now a state observation with exact declaration/use
lines, with genuine settings and Session axes retained; the unchanged live comment
previously producing one quote-style finding now produces zero. Test Derivation
therefore moves from 8 to 9 within supported static evidence scope. Business Rules
remains 8: a blinded audit found 12 directly entailed and six properly qualified
rules out of 18, but single-line VB If still limits structured return evidence.
Its parser follow-up is deployed in V63 above; fresh semantic failures remain explicit.

V61 was the preceding installed release and passed live deployment acceptance. Forty-seven focused tests
passed, and actual MCP comparison replaced an unsupported refactor label with an
unclassified narrative (output schema 2, all 147 request schemas unchanged).
The nullable-field matrix correction passed its generic regressions, but a live
reference check exposed an additional case: an unrelated nested class caused the
recognizer to withhold the correction. That failed acceptance is preserved.
A scope-aware follow-up and nested/cross-class regressions passed in V62 above;
Test Derivation remained 8 until that live acceptance. The daemon, configuration, parser, source and generation
passed deployment preservation checks; the historical comparison remains on V56.

V60 was the preceding installed release and passed live acceptance. Empty immune-rule and anti-pattern
corpora now produce degraded gate coverage, preserving destructive-pattern
findings without inventing violations. Thirty-nine relevant tests passed, followed
by release compilation and actual old/new MCP comparison on an existing recorded
fixture: both gates changed from passed to degraded with zero fabricated findings.
All 147 schemas, stored fixture documents and source remained unchanged. Deployment
preserved configuration, parser, stored live knowledge and application source.
The temporary reduction of Pre-commit Review to 8 is resolved by this evidence;
its restored 9 does not certify complete semantic review or runtime behavior.

The native comparison and all twelve registered QA role runs are now sealed,
with limitations. Both treatment testers consumed
method context, business rules, test-matrix and review tools, and explicitly
refuted misleading outputs rather than accepting them. Two generic defects were
corrected after the frozen run: a nullable cache-member presence check misclassified as a
configuration setting, and modified-only diffs described as behavior-preserving
refactors. These changes are not part of the frozen V56 comparison. Business
Rules remained 8 pending stronger semantic/workflow evidence; Test Derivation's
later increase is supported by V62 live acceptance, not tool delivery alone.

The matrix and narrative corrections passed 47 focused tests with unchanged
source pins; V61 release verification passed as described above. The treatment code reviewer
also refuted 19 reported review findings as inapplicable or unsupported. Its
report independently corrected inaccurate source locations and an overstated
compiler claim in the historical-rules brief. Independent provenance audits found
unsupported direct-quotation claims in both arms. Comment apostrophes treated as
string quotes are repaired in V62; incomplete existing-class sampling remains a
product improvement target. The sealed comparison establishes useful evidence
delivery, but no unique true defect caught or prevented by Engram and no demonstrated
comparative correctness or productivity advantage on this case.

V59 was the preceding installed release and passed live acceptance. Review input resolution now accepts
multiline inline diffs ending in `.patch` or `.diff`, and project-relative patch
files use the scoped read helper. Forty relevant tests passed, including an
observed failure before the correction. Actual MCP checks verified the regression,
external-path rejection and equivalent findings/gate outcomes for the same sealed
four-file patch supplied inline or by file. All 147 schemas remained unchanged;
deployment preserved configuration, parser, stored knowledge and application source.
This input-contract improvement does not establish semantic review completeness.

V58 was the preceding installed release and passed live acceptance without an index refresh. Seven
focused availability/freshness tests, release compilation and actual MCP acceptance
passed. Intake now reports missing historical audit evidence independently of
source freshness, and distinguishes record presence from validation. The historical
candidate correctly reports both stale source and an empty anti-pattern corpus;
generation, knowledge counts and all 147 schemas remain unchanged. Deployment
preserved configuration, parser, stored knowledge and application source.

V57 was the preceding installed release and passed live acceptance without an index refresh. Its
case-sensitive matrix identity correction passed 11 affected integration tests,
release compilation and actual MCP acceptance: 16 dependency rows preserved,
the formerly dropped case-distinct request now diagnosed, 147 schemas unchanged,
and private generation 3 unchanged. Wider V56 evidence is inherited, not rerun.

Both native implementation sessions finished with exit 0 and sealed outputs,
using one registered foreground developer each and their own sealed plans. Each
changed only the four admitted files; generated JavaScript came from the actual
compiler. No new compiler diagnostics were introduced, but the original batch
remains FAIL because of pre-existing diagnostics and unavailable guard coverage.
The comparison stayed pinned to V56/Luna versus no Engram. Implementation-stage
cost/time was $10.64/716 seconds baseline and $14.25/837 seconds with Engram.
These are not speed-benefit evidence; harness friction is recorded separately.
Both candidates subsequently compiled independently and passed 212 controller
browser checks across the stated access, filtering and menu-state scope. Native
Fable tester/gate review and the comparative assessment are complete with limitations;
the complete role/setting/FACT/theme/shared-master matrix is not established.
Implementation plus native QA cost $37.37 baseline and $47.04 with Engram, excluding
intake, preparation, controller testing and indexing/provider work. Both candidates
support a reasonable interpretation of the sparse original text. Both omit the
unresolved grouping affordance and the later explicit navigation/grouping scope;
later comments were excluded from the original intake. Neither candidate is certified
as complete production delivery. The approved implementation is a reference, not
an infallible correctness oracle.

Before native QA, the controller refreshed only the sealed treatment candidate's
four changed source files with archived V56. Generation 4 contains all 2,199
eligible paths; zero history commits were processed. Full stored-document
fingerprints preserved all 124,828 historical guidance documents, excluding the
generated index report. Source bytes stayed sealed and the held-out commit stayed
absent. QA therefore uses the completed candidate's current index, with the same
engine/model and historical evidence. The initial acceptance harness expected the
wrong freshness label; its failure is retained and a separate read-only acceptance
verified the actual `no_changes_detected` response without repeating the refresh.

The replay exposed an empty historical anti-pattern corpus. V58 now surfaces
that missing prerequisite in freshness and health responses, separately from
source freshness. Record presence is explicitly not validation. Seven focused
tests pass, including add/remove transitions and unknown-count handling. Release,
actual MCP acceptance and deployment passed as recorded above. The native pair
remains pinned to V56; its implementation logs contain 25 Engram tool deliveries.

V56 was the preceding installed release and passed live acceptance without an index refresh. It passed
**1,495 tests**, release compilation and all 147 unchanged schemas. Verified
business-rule results now offer a bounded caller-context action so agents can
check caller-supplied arguments against a method's parameters and defaults.
The actual historical MCP handoff recovered source-verified caller excerpts,
including explicit arguments that narrow the helper's behavior. Stale target
source suppresses the action; stale caller source is withheld. This does not
claim exhaustive call resolution or automatic argument binding. The private
native host is also verified to use the accepted Luna configuration.

Both isolated runtime fixtures now pass normal authentication and dashboard
delivery for all five actors. Their rendered project sets match at 8/0/3/3/0,
including restricted and empty-list cases. This establishes the runtime fixture
baseline, not JavaScript behavior or candidate implementation acceptance.
Subsequent actual browser checks matched four shared menu states in both
untouched instances, including the server-selected header's first-click-open,
second-click-collapse behavior. These are baseline observations, not filter QA.

V57 preserves case-sensitive graph file identities during matrix
request/companion deduplication, including preservation of a missing-identity
diagnostic that was silently dropped.

V55 was the preceding installed release and passed live acceptance without an index refresh. It passed
**1,465 tests**, release compilation, and all 147 unchanged schemas. The original
four-file UI-only matrix request now returns **16 dependency rows**, exactly
matching the separately requested code-behind control, in 0.26 seconds. The
existing private index remained generation 3 and no LLM calls were made. Generic
regressions cover declared companions, commented directives, missing targets,
stale markup, stale companion source and project-root escape. The tool labels
the companion as context, keeps the changed-file set intact and does not infer
transitive helpers or claim tests executed. Live deployment preserved memory
sections, repository rules, configuration, parser and application source.

Both native synthesis correction sessions completed (baseline 242 seconds/$2.03,
Engram 451 seconds/$3.30; stage-local, including correction friction). Independent
review finds both plans sufficiently coherent to prepare isolated implementation,
with explicit test-oracle/compiler qualifications. The prior alleged business-rule
line offsets are not established defects: those citations identify Else branch
conditions rather than subsequent consequence statements. Caller-specific argument
context remains a workflow composition gap, not proof that method-scoped rules
are false. Subsequent implementation completion is reported above; runtime
acceptance remains separate.

V54 was the preceding installed release and migrated the live project to generation **1263** (source
format 6, all 2,270 eligible paths present, no pending disk changes detected).
It passed **2,359 tests** (901 index and 1,458 server), release compilation,
147 unchanged schemas, and actual historical MCP acceptance. The exact
class-qualified request that failed during native planning now returns the
correct method body, matching source line for line. Parser declaration identity
is keyed by source position; ambiguous short names no longer inherit the last
class's identity. Three property-state owners and three business-rule documents
remain intact. The private refresh took 53 seconds and grew the existing index
by about 93 MiB, with no copy. Live refresh took 143 seconds; all 298
non-generated knowledge sections and repository rules were preserved.

The native synthesis pair completed: baseline 24 calls, 341 seconds, reported
$2.36; Engram 26 calls, 375 seconds, reported $3.73. These are stage-local costs,
excluding preparation and earlier stages. The Engram arm actually re-ran matrix
queries and dispositioned the repaired property evidence. Controller review
found behavior/spec contradictions in both plans; first attempts were sealed
before bounded independent corrections. No implementation or general causal
quality advantage has yet been established.

V53 was the preceding release and its live source-format migration passed. It passed **2,333
tests**, release compilation, all 147 unchanged schemas, and actual historical
MCP acceptance. Three previously misattributed Session dependencies now belong
to their properties in both the matrix and state tracing. All three historical
business-rule documents survived that refresh. The private index grew by about
190 MiB, with no new index copy.

Binary-only deployment preserved all 299 memory sections and repository rules.
The separate live migration completed in 167 seconds at generation **1262**:
all **2,270 eligible paths** are present, source format 5 is current, and no
pending disk changes were detected. All 298 non-generated knowledge sections
and repository rules matched; only `engram/index_report` could refresh. Parser,
Luna configuration and application source were unchanged. V53 includes V52's
bounded startup warm-up. The class-identity failure was subsequently repaired in V54.

V52 passed **1,430 tests** (1,424 unit tests and six warm-up integration tests),
release compilation, and actual MCP acceptance with 147 unchanged schemas and
identical retrieval results on the recorded intake. It bounds startup warm-up to
the runtime cache capacity of five; a seven-project regression verifies that
older projects allocate no startup-derived caches and still search on demand.
V52 was archived without a separate deployment; its changes are included in V53.

The native planning pair completed with actual registered planners: 82 baseline
tool calls and 74 Engram-arm calls, including 16 child MCP deliveries. Fifteen
MCP calls returned successfully; setting analysis failed because the private
host lacked its Luna configuration. A preserved same-parent-session repair
restored that dependency and recovered four additional test scenarios from a
bounded helper-file matrix. Native child resume was unavailable, so these later
calls are parent deliveries. Both arms found shared script consumers and access
boundaries; no general causal superiority or implementation success is claimed.
Search/chunk remains 9 and business rules remains 8. The new false state-owner
evidence lowered test derivation to 8. Ownership is now repaired, but the
UI-only coverage gap and full implementation/runtime verification remain open.

V51 was the preceding installed release. Its generic full-capture normalization passed **1,426 tests**
(1,424 unit tests and two intake integration tests), release acceptance on the
exact native input, all 147 unchanged schemas, and guarded live deployment.
Generation 1261 remained healthy and unchanged; memory-bank bodies and repository
rules matched before and after. Luna configuration and the parser are unchanged.
The earlier V51 release acceptance compared schema array order unnecessarily;
the preserved failure was followed by a successful comparison of all schemas by
tool name. Candidate ranks and image invariance also matched the debug results.

V50 passed **1,469 selected tests**, release compilation, and actual MCP acceptance:
six successful tool calls, one expected invalid-target rejection, and all 147
advertised schemas. V50 was the preceding installed release. Separate live acceptance verified its binary,
all 147 schemas, source retrieval, and preservation of 299 memory sections and
repository rules. The initial deployment receipt retained a failed checkout
freshness check; a subsequent incremental refresh passed at generation 1261 with
all 2,270 eligible paths present and no pending source changes at that check.

The combined changes:

- Preserve original rich-text intake while using one normalized retrieval view
  for concepts, history and semantic search. The historical image-only control
  now returns identical candidate files, signals, ranking and omissions. Before
  the fix, seven semantic candidates appeared only with the image tag and two
  only without it. A generic regression also reproduces history recall lost to
  raw HTML query syntax.
- Distinguish explicit source ranges from indexed method bodies. Invalid bounds
  fail instead of returning empty or clipped text. Mixed FQN/range targets fail.
- Verify and slice the target method from one loaded file snapshot, expose its
  BLAKE3 hash, and identify missing legacy fingerprints without claiming a
  successful verification. Legacy Markdown does not label an unverified span a
  full method body.
- Keep Luna and the v10 member template unchanged. Supporting execution-stage
  guidance has its own version in the analysis fingerprint and distinguishes
  returning deferred work from executing it. Legacy evidence remains readable
  and is not silently upgraded.

The first v48 acceptance harness failed on the existing trimming of outer intake
whitespace. Its corrected rerun and the original failure are both retained.
During v50 development, the legacy fixture initially used a metadata-preserving
upsert instead of clearing its fingerprint; the fixture was corrected and all
11 method/caller tests passed. These were not successful first attempts.

## Business-rule evidence and limitations

Three historical navigation/access methods were extracted and retrieved using
Luna/v10. Manual review covered purpose, steps, rules, data flow, error handling
and side effects. The main access/filter branches were supported, but one output
incorrectly required query enumeration before returning an IQueryable.

One new extraction after the versioned supporting-guidance change corrected that
specific deferred-execution error. The error-handling word "otherwise" remains
ambiguous and must not be read as exception recovery. Original and changed outputs
are retained. No semantic approval was written into Engram and no selected reroll
replaced the first result. This is a bounded improvement, not a statistical result.

Three earlier native Fable tester probes recovered the required tools and complete
source/analysis but did not explicitly reject an overbroad legacy prose claim.
Business Rules therefore remains 8. Full paired Fable workflow advantage has not
been established; historical replay setup and tool diagnostics are not an
implementation/runtime acceptance result.

The next paired native intake completed with the complete applicable Fable
process and original hook roster, explicitly adapted for historical paths and
captured knowledge. Both arms independently verified their isolated base runtimes.
The Engram arm has delivered actual freshness, dossier, history, semantic search,
grep and chunk responses. Initial artifacts and native transcripts are hash-sealed
before corrective continuation. Reported native costs were $4.10 baseline and
$4.61 with Engram, with approximately 435 and 438 seconds elapsed respectively;
these exclude coordinator preparation costs and do not establish an advantage.
Both arms found a shared territory ownership gap. The Engram dossier additionally
exposed capture-label pollution: metadata displaced useful search concepts and
buried relevant files. The generic candidate passed 1,424 unit tests and two
integration tests. Replaying the exact native request moved the central markup
from rank 71 to 1 and its code-behind from omitted to rank 5. Its client source
changed from absent to rank 108; 161 candidates remain, so output relevance and
size still need improvement. Image metadata removal leaves candidate results
unchanged. Release validation and live deployment passed; this fix is deployed.
Native transcript review verified all 3,338 dossier lines were delivered
through five successful reads, and every observed Engram call had a non-error
response. Delivery success does not establish semantic usefulness or causal benefit.
Harness restrictions and their correction costs are kept separate from product
defects and agent performance.

## Preserved infrastructure evidence

V47 initialized 154,754 vectors in the existing private historical index without
changing its 44 stored-text files. A separate provider-failure test after staging
data existed left no published or abandoned staging table and preserved the text
corpus. This is synchronous maintenance, not a cancellable background job.

The live source refresh reached healthy generation 1261 with all 2,270 eligible
paths present. Earlier failed freshness receipts remain preserved. Memory-body
preservation was checked before that refresh; the generated index report changes
with refresh and a second full memory comparison has not been claimed.

Detailed evidence is retained outside the repository under the existing
`fable-workflow-20260910` and historical replay audit roots: v50 validation,
`intake-ranges-v50`, `business-v48`, `business-v50`, and the deployment proposal.
No new reference-source/index copy, SQL database or cleanup deletion was created
for this coding pass. The stable build directory and private index were reused.
