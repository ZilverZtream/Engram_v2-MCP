# Workflow contract review, 2026-09-10

Reviewing the actual phase and role contracts exposed missing evidence reported as
success, incomplete tool grants, and lost identity across planning and review.
The generic Engram repairs were deployed at 09:59 UTC. This is tool-contract
acceptance, not certification of the complete agent workflow or agent advantage.

The repair covers checkout freshness, sparse-item intake, packed context coverage,
atomic edit-session ownership, preview-only setting descriptions, bounded rule
recovery, untracked patch contents and diff parsing, insufficient immune evidence,
directory guard scope, setting identity in test matrices, completeness reporting,
composite co-change provenance, and exact file/method identity in review gates.

Validation passed 1,296 server unit tests, 118 integration tests across 20 suites,
and four registry tests. Six failures were reproduced using the previous compiled
server. Eight compiled MCP checks passed using the replacement, including session
lifecycle and composite planning evidence. Failed intermediate validation receipts
are retained with their dispositions; they are not release evidence.

Installed SHA256:
`80a3adc055d3ff205eb782c3f46b906a724ac6e0f114ec9d1491033560cff682`.
Live acceptance retained all 147 tool names, healthy stores and unchanged config.
Six schema changes are recorded in the deployment receipt. A rollback binary is
retained. Production model configuration was not changed.

The workflow inventory covers 55 referenced tools and 55 role/template files.
It does not establish new end-to-end coverage of all 55 tools. An isolated role
converter proposal passes 48-role fixtures, required-tool and preload validation,
catalog-superset acceptance, byte preservation and no-write-on-invalid-input checks.
The review-gate proposal passes 12 mocked-host tests for deduplication, disagreement,
partial verification and invalid findings. Neither proposal is installed or host
certified. Missing phase implementations and domain charters remain integration work.

Static gates cannot establish runtime invocation or requirements completeness.
Business-rule semantic coverage remains incomplete. Existing replay experiments
used a different protocol and cannot establish this workflow's agent benefit.
No reference application files were copied into Engram documentation.

Detailed phase audit, proposals, source manifests, before/after MCP responses,
validation logs and deployment receipts:
`C:/ai-projects/audits/Engram/fable-workflow-20260910/`.
